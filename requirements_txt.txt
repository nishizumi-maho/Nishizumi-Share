# Core Dependencies
Flask>=3.0.0
waitress>=2.1.2
requests>=2.31.0
PySocks>=1.7.1

# UI Framework
PyQt6>=6.6.0

# Optional but recommended
# ClamAV for Linux/macOS (install via package manager)
# Windows Defender (included with Windows)
